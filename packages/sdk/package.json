{
  "name": "@substrate/sdk",
  "version": "1.0.0",
  "description": "Orcastr8r TypeScript SDK — programmatic access to agents, executions, and fleet",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^20.0.0"
  }
}
