{
  "name": "@askalf/standalone",
  "version": "2.9.9",
  "description": "AskAlf standalone server — no Docker, no PostgreSQL, no Redis. Just Node.js.",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "askalf": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@askalf/database-adapter": "workspace:*",
    "@askalf/redis-adapter": "workspace:*",
    "fastify": "^5.8.4"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsx": "^4.0.0"
  }
}
